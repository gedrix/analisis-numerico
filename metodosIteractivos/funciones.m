%funciones de x,y en Euler y heun
function [dy] = funciones(x, y)
    %dy = x + ((1/5)*y);
    %dy =  x^3 - x- 1;
   % dy = 68*x^5 + 5*x^4 -22*x^3+13*x^2-102*x +66;
   %dy = cos(2*x) + sin(3*x);
   %dy = cos(2*x) + sin(3*y);
   dy = (1-2*x) * (y);  
   %valores de esa ecuacion a=0, b=3, b=16,y=0
end
